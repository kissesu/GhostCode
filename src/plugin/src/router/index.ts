/**
 * @file 路由模块导出
 * @description 多模型路由模块的公共 API
 * @author Atlas.oi
 * @date 2026-03-02
 */
export { resolveRoute } from './strategy';
export type { RouteDecision, BackendName, ModelRouting, RoutingStrategy } from './types';
